# Same idea as versusy-admin VITE_API_BASE: API path prefix or full base URL (no trailing slash).
# Dev default in code is `/api` (CRA proxy in package.json forwards to your backend).
# REACT_APP_API_BASE=https://your-api.example.com/api
