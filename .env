# Baked into the bundle at build time. Restart `npm start` after changes.
# If your API uses a servlet context path `/api`, use: ...run.app/api
REACT_APP_API_BASE=https://v1-versusy-928473098122.europe-west1.run.app/api
